## 🐍 Snake Game — клиент–серверная игра «Змейка» (Spring Boot + Java Swing)

Проект представляет собой полностью реализованную клиент–серверную игру «Змейка».

Серверная часть написана на Spring Boot, а клиент — это графическое приложение на Java Swing, которое отображает состояние игры и обрабатывает действия игрока.

## 🚀 Функциональность

#### 🖥 Сервер (Spring Boot)

Обработка HTTP-запросов от клиента (REST API).

Инициализация игровой сессии.

Расчёт логики игры: движение змейки, генерация еды, столкновения.

Stateless-взаимодействие — клиент отправляет только действие, сервер возвращает полное состояние.

Работа с БД: PostgreSQL (основная), H2 (dev/test).


#### 🎮 Клиент (Java Swing)

Графическая отрисовка игрового поля в окне Swing (JFrame).

Обновление состояния игры на основе ответа сервера.

Отправка действий игрока (направление движения) на сервер.

Использование Java HTTP Client для REST-запросов.

JSON-сериализация/десериализация с помощью Gson.

Обработка нажатий клавиш для управления змейкой.

Компонент Game получает данные через GameSnakeClient, запускается через MainWindow.

Пример основного окна:

    public class MainWindow extends JFrame {
      public MainWindow(){
          setTitle("Змейка");
          setDefaultCloseOperation(WindowConstants.EXIT_ON_CLOSE);
          setSize(355,375);
          setLocation(400,400);
          add(new Game(new GameSnakeClient()));
          setVisible(true);
      }
      public static void main(String[] args) {
          new MainWindow();
      }
    }


## 🧩 Технический стек
Сервер:
Java 21, 
Spring Boot 3.5, 
Spring Web, 
Spring Data JPA, 
PostgreSQL / H2, 
Lombok, 
Maven.

Клиент:
Java 21, 
Java Swing — UI, 
Gson — работа с JSON, 
Java HTTP Client — взаимодействие с REST API, 
Maven.

## 🧪 Тестирование

Сервер:
JUnit 5, 
Mockito, 
Testcontainers (PostgreSQL).

Клиент:

JUnit 5 — тестирование логики игры и сетевого клиента

Mockito — мокирование запросов к серверу

## 📦 Сборка и запуск
Сервер:
mvn spring-boot:run

Клиент:
mvn compile exec:java -Dexec.mainClass="MainWindow"
