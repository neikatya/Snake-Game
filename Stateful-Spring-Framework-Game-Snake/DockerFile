FROM bitnami/postgresql:16.4.0

ENV POSTGRESQL_POSTGRES_PASSWORD=pwd \
    POSTGRESQL_USERNAME=postgres \
    POSTGRESQL_PASSWORD=pwd \
    POSTGRESQL_PASSWORD_ENCRYPTION=md5 \
    POSTGRESQL_PGAUDIT_LOG="all, -misc"

USER root
